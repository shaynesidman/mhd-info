---
import NavBarItem from "./NavBarItem.astro";
---

<nav>
    <div class="flex flex-row justify-between items-center bg-[var(--alt)] px-4 mt-1 rounded-3xl">
        <!-- Logo -->
        <div>
            MHD
        </div>
        <!-- Tabs -->
        <div class="flex flex-row gap-2 py-1">
            <NavBarItem text="Spec" />
            <NavBarItem text="b" />
            <NavBarItem text="c" />
            <NavBarItem text="d" />
            <NavBarItem text="e" />
        </div>
    </div>
</nav>